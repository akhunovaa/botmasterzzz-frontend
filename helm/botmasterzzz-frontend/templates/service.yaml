apiVersion: v1
kind: Service
metadata:
  name: {{ include "fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      name: http
  selector:
    app: botmasterzzz-frontend